(windows): download python version Python 3.12.5+ and make sure python PATH is defined in system variables

pip install pytest
pip install pytest-playwright
pip install playwright
playwright install

to run from cmd from downloaded repo folder: pytest -s -vv
